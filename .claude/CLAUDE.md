At the start of every session, apply the i-have-adhd skill's output rules automatically — do not wait for the user to invoke it.

Don't add code comments. If one is truly necessary, keep it terse — half a sentence, max — unless something genuinely novel/non-obvious demands more.

Git commit messages: use Conventional Commits format (`fix:`, `feat:`, `chore:`, `refactor:`, `docs:`, etc.) for the subject line.

Pull requests: keep descriptions very succinct — a line or two, not multi-section Summary/Test plan write-ups, unless asked for more detail.
